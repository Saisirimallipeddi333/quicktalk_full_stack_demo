QuickTalk – Full Tech Stack Demo

Included (at least minimally used):
- Java, Spring Boot
- WebSocket (STOMP)
- React (Vite, TS)
- MySQL (JPA entity + repo)
- Redis (dependency + docker service)
- WebRTC (TS skeleton)
- JWT (auth filter + service)
- Docker + docker-compose
- MinIO (media upload service + docker)
- Leaflet (map in React)
- BouncyCastle (E2EE keypair demo)

This is a demo/starter, not a production-perfect app, but it shows all tools in one repo.
